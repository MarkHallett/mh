"""An example of an internal module within the package.

Although the internals are not explicitly exposed, they are still available via

    import mhlib
    mhlib.eg.MhEg().run_mh_eg()

    import mhlib.l2.l2
    mhlib.l2.l2.L2EG_VAR
"""

